const name = 'my name is judah';

console.log(name);

for (let i = 0; i < name.length; i++) {
  console.log(name[i]);
}

let index = 0;

while (index < name.length) {
  console.log(name[index]);
  index = index + 1;
}
---------------------------------------------------

client.addToRole({
	serverID: Snowflake,
	roleID: Snowflake,
	userID: Snowflake,
});

----------------------------------------------------
<@${judy}>